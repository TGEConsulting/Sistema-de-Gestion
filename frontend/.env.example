VITE_API_URL=http://localhost:4000/api

# Login con Google (Google Cloud Console → OAuth 2.0 Client ID). Debe ser el
# MISMO valor que GOOGLE_CLIENT_ID en el backend. Si se deja vacío, el botón
# de "Iniciar sesión con Google" simplemente no aparece.
VITE_GOOGLE_CLIENT_ID=
